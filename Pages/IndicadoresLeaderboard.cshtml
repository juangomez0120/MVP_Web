@page
@model Sprint3.Pages.IndicadoresLeaderboardModel
@{
    ViewData["Title"] = "IndicadoresLeaderboard";
}

<div class="container">
    <!-- Leaderboard -->
    <div>

        <!-- Escoger Opción: Leaderboard de Examen o Entrenamiento o Indicadores-->
        <div class="centered row top" style="font-family: 'Oswald', sans-serif; font-size: 24px;">

            <div class="col s5">
                <a href="@Url.Page("/EntrenamientoLeaderboard")" class="greyForRanking">Entrenamiento</a>
            </div>

            <div class="col s3">
                <a href="@Url.Page("/ExamenLeaderboard")" class="greyForRanking">Examen</a>
            </div>

            <div class="col s4">
                <a href="@Url.Page("/IndicadoresLeaderboard")" class="red1">Indicadores</a>
            </div>

        </div>

        <div class="top">
            <table class="centered greyDark">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Nombre</th>
                        <th>Tipo</th>
                        <th>Intentos</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ListaIndicadores)
                    {
                        <tr>
                            <!-- Lugar -->
                            <td>
                                @Html.DisplayFor(modelItem => item.posicion)
                            </td>

                            <!-- Nombre -->
                            <td>
                                @Html.DisplayFor(modelItem => item.nombre)
                            </td>

                            <!-- Tipo de Chatarra -->
                            <td>
                                @Html.DisplayFor(modelItem => item.tipo)
                            </td>

                            <!-- Intentos -->
                            <td>
                                @Html.DisplayFor(modelItem => item.intentos)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>